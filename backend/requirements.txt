# =============================================================================
# SKREENIT LOCAL SERVER REQUIREMENTS
# Optimized for: Intel Xeon E-2276M + 64GB RAM + NVIDIA Quadro T2000 (4GB)
# =============================================================================

# =============================================================================
# PYTORCH WITH GPU SUPPORT (CUDA 12.1)
# For Quadro T2000 with 4GB VRAM (CUDA 12.8 installed, using 12.1 wheels)
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.2.0+cu121
torchvision==0.17.0+cu121
torchaudio==2.2.0+cu121

# =============================================================================
# CORE BACKEND
# =============================================================================
fastapi==0.110.0
uvicorn[standard]==0.27.1
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.6.3
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
sqlalchemy==2.0.28
pymysql==1.1.0
jinja2==3.1.3
email-validator==2.1.1
PyJWT==2.8.0
websockets==12.0

# =============================================================================
# CLOUD & EMAIL
# =============================================================================
boto3==1.34.69
resend==0.8.0

# =============================================================================
# VIDEO PROCESSING & ML STACK (GPU Accelerated)
# =============================================================================
# Whisper (OpenAI) - Speech recognition
git+https://github.com/openai/whisper.git

# GPU-accelerated ONNX Runtime
onnxruntime-gpu==1.17.1

# MediaPipe - Face detection, pose estimation
mediapipe==0.10.14

# OpenCV with GPU support (for Quadro)
opencv-python==4.8.1.78

# Pillow for image processing
pillow==10.2.0

# FFmpeg wrapper for Python
ffmpeg-python==0.2.0

# MoviePy - Video editing/processing
moviepy==1.0.3

# FER - Facial Expression Recognition (uses GPU)
fer==22.5.0

# InsightFace - Advanced face analysis
git+https://github.com/deepinsight/insightface.git#subdirectory=python-package

# =============================================================================
# NLP & TEXT PROCESSING
# =============================================================================
textblob==0.18.0.post0
nltk==3.8.1
textstat==0.7.3
scipy==1.12.0
soundfile==0.11.0
eng-to-ipa==0.0.2
numpy==1.26.4  # Pinned for compatibility
sentence-transformers==2.5.1  # For semantic similarity
transformers==4.38.2  # Hugging Face transformers
accelerate==0.27.2  # For model acceleration

# =============================================================================
# DOCUMENT PROCESSING
# =============================================================================
PyMuPDF==1.23.26
python-docx==0.8.11
reportlab==4.1.0
pdf2image==1.17.0
pdfplumber==0.10.4

# =============================================================================
# SYSTEM UTILITIES
# =============================================================================
psutil==5.9.8  # System monitoring
GPUtil==1.4.0  # GPU monitoring
pynvml==11.5.0  # NVIDIA management library

# =============================================================================
# PERFORMANCE OPTIMIZATION (for 64GB RAM)
# =============================================================================
# Ray - Distributed computing (can utilize all CPU cores)
ray==2.9.3

# Numba - JIT compilation for Python
numba==0.59.1

# Dask - Parallel computing
# dask==2024.3.1
# distributed==2024.3.1

# =============================================================================
# MONITORING & LOGGING
# =============================================================================
structlog==24.1.0
prometheus-client==0.20.0

# =============================================================================
# NOTES FOR DEPLOYMENT
# =============================================================================
# 1. Install CUDA 12.8 toolkit on your machine (PyTorch uses cu121 which is compatible)
# 2. Verify GPU: python -c "import torch; print(torch.cuda.is_available())"
# 3. Ollama should be installed separately from https://ollama.com
# 4. This configuration can process multiple videos simultaneously
# =============================================================================
